I used ParticleDynamics_Code_Evaluation.txt as the active report and mapped it against the actual repo. One material mismatch is that the report explicitly says it could not inspect the source tree, so I treated its priority order and PR skeleton as the scope guard, but not its repo-state guesses as authoritative. Current baseline on this machine is Julia 1.12.3 with CUDA.functional() == true; `julia --project -e 'using Pkg; Pkg.test()'` is currently red with 490 passed / 1 failed, the failure being the slowdown guardrail in `test/test_bonded_exclusions.jl`. The repo is already partly through Phase 1: `src/ParticleDynamics.jl`, `docs/`, and `.github/workflows/` exist, so the "rename" PR is now residue cleanup, not a first-time rename.

| Branch name | Draft PR title | Report phase / issue | Primary files changed | Tests to add or update | Benchmarks / perf checks | Acceptance criteria | Rollback plan |
|---|---|---|---|---|---|---|---|
| `test/00-baseline-guardrail` | Characterize and stabilize the current GPU baseline | Phase 0 safety and characterization; current `Pkg.test()` failure in bonded-exclusion slowdown guardrail | `test/test_bonded_exclusions.jl`, `test/utils.jl`, `benchmarks/Argon_3D_NVT_NHC_basic.jl` | Update polymer slowdown guardrail; add stable characterization helper for the soft-repulsive bonded path | Rerun guardrail timings; reduced-step `benchmarks/Argon_3D_NVT_NHC_basic.jl` | Baseline is trustworthy: either the guardrail passes or it is replaced with a stable regression check that still catches real slowdowns | Revert the guardrail PR and return to today's known-red / known-noisy baseline |
| `repo/01-release-hygiene` | Finish ParticleDynamics release-facing rename and repo hygiene | Phase 1 repository cleanup / rename residue | `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `.github/workflows/ci-cpu.yml`, `.github/workflows/ci-gpu-selfhosted.yml`, `.travis.yml`, `.appveyor.yml` | Import smoke, docs build smoke, example smoke script | None; import/docs/example smoke only | No tracked release-facing file still points to `NonEqSimGPU` or `NEWS.md`; CI targets the chosen base branch; `CHANGELOG.md` exists | Revert the hygiene PR; runtime code stays untouched |
| `refactor/02-public-api-boundary` | Define the supported public API and de-export internals | Phase 2 public/private API boundary; P0 minimum public API | `src/ParticleDynamics.jl`, `README.md`, `docs/src/index.md`, `docs/src/manual/getting_started.md`, `test/test_api.jl`, `examples/2D_allpairs_quicktest.jl`, `examples/3D_quicktest.jl` | Tighten export/API smoke tests; update curated public example smoke | None; API/docs only | Export list is explicit and documented; curated public examples use only supported top-level API; any cheap shims are documented | Restore prior exports and docs/example wording |
| `refactor/03-split-simulation-state-build` | Extract state and build responsibilities out of `Simulation.jl` | Phase 3 split `Simulation.jl` | `src/Simulation.jl`, `src/SimulationState.jl`, `src/SimulationBuild.jl`, `src/ParticleDynamics.jl`, `test/test_build.jl`, `test/test_virial.jl`, `test/test_ir_phase2.jl` | State-layout/build/regression tests; virial smoke | Reduced-step `benchmarks/Argon_3D_NVT_NHC_basic.jl` | `SimulationState` and build helpers live outside `Simulation.jl`; shared step engine remains behaviorally equivalent on deterministic cases | Revert the extraction PR and restore original include wiring |
| `refactor/04-backend-boundary` | Introduce backend traits and storage adaptation at the build boundary | Phase 4 backend-specific isolation | `src/Backends.jl`, `src/ParticleDynamics.jl`, `src/SimulationBuild.jl`, `src/Simulation.jl`, `test/test_build.jl`, `test/test_api.jl` | Backend-selection construction tests; `allowscalar(false)` smoke; CPU reference build tests if approved | Reduced-step `benchmarks/Argon_3D_NVT_NHC_basic.jl`; selected Phase 4A force/neighbor tests | Default CUDA path is unchanged; builders choose backend concretely; no hot-kernel rewrites; performance stays within agreed threshold | Revert the backend PR and keep CUDA-only builders |
| `test/05-force-gpu-harness` | Expand force correctness and GPU-residency regression coverage | Phase 0 validation-first suite / concrete harness PR | `test/test_phase4a_forces.jl`, `test/test_phase4a_neighbors.jl`, `test/test_bonded_exclusions.jl`, `test/test_ir_phase2.jl`, `test/runtests.jl` | Add pairwise/regression coverage for touched force and residency paths | Rerun guardrail timings; quick Argon benchmark smoke; optional NIST spot check | Refactor-relevant force/storage paths have explicit regression coverage; CUDA scalar indexing still fails loudly | Revert the harness PR; core engine unchanged |

1. `test/00-baseline-guardrail`: Goal is to make the baseline trustworthy before refactors. In scope is the existing slowdown guardrail and a matching characterization harness; out of scope is broad force redesign. This stays reviewable because it is test/benchmark-centered; the main risk is that it uncovers a real hotspot, and if that happens it can touch hot GPU code, but it should not change user-facing API.

2. `repo/01-release-hygiene`: Goal is to finish the repo-facing rename and release prep that is still inconsistent today. In scope are stale names, changelog creation, and CI/base-branch cleanup; out of scope are algorithm changes and broad dependency slimming. This is small and safe because it does not touch hot paths; it should not change user-facing API.

3. `refactor/02-public-api-boundary`: Goal is to freeze what the package actually promises at top level, especially around `src/ParticleDynamics.jl`. In scope are exports, docs wording, and curated public examples/tests; out of scope are integrator math, storage layout, or kernel behavior. This is reviewable because it stays at the module/documentation boundary; it does not touch hot GPU paths, but it may change user-facing API if we remove accidental exports or add a few cheap shims.

4. `refactor/03-split-simulation-state-build`: Goal is to take the first safe slice out of `src/Simulation.jl` by moving state/build concerns first and leaving the step engine in place. In scope are `SimulationState`, build helpers, and related validation/layout code; out of scope are stepping math, neighbor rebuild algorithms, and writer semantics. This is small enough because it does not attempt the full architectural end-state in one PR; risk is wiring drift, and it touches a hot-path-owning file but not the kernels themselves.

5. `refactor/04-backend-boundary`: Goal is to introduce a thin internal backend layer without abstracting kernels. In scope are backend marker types and storage-adaptation/build boundaries; out of scope are a production CPU backend, KernelAbstractions rewrites, or portability work inside kernels. This is reviewable because it stops at construction/adaptation; it touches hot GPU-adjacent code and may add a user-facing `backend=` keyword, but the default path should remain backward compatible.

6. `test/05-force-gpu-harness`: Goal is to protect the refactors with explicit force/residency regression checks. In scope are tests and only tiny hooks if absolutely necessary; out of scope is redesigning `NonBondedForces.jl`. This stays small because it is mostly test-only; it should not change user-facing API and should avoid touching hot GPU code beyond minimal observability hooks.

I am intentionally not planning a Phase 6 `NonBondedForces.jl` redesign in this run. The report's own concrete-first PR plan front-loads API/state/backend/harness work, and trying to redesign `src/NonBondedForces.jl` before those boundaries land would not stay reviewable.

1. What is the base branch for this edit phase? I am currently on `master`, while both GitHub workflows trigger on `main`.
2. Should I create actual remote draft PRs if credentials and CLI access are available, or should I limit myself to local branches plus saved PR markdown files?
3. The working tree currently has `ParticleDynamics_Code_Evaluation.txt`, while `evaluation_report.txt` is deleted locally but still tracked in `HEAD`. If their content differs, which one should win?
4. If tracked repo-facing surfaces still contain the legacy name `NonEqSimGPU`, do you want that rename residue handled now only where the report keeps it high priority?
5. Which report priorities are truly in scope for this run: only P0 / must-fix-before-public-release items, or P0 plus selected P1 items?
6. What is the maximum acceptable performance regression on touched hot GPU paths: `0%`, `3%`, `5%`, or another threshold?
7. Which benchmark suite or workload should be the performance gate for this run: `benchmarks/Argon_3D_NVT_NHC_basic.jl`, `benchmarks/Argon_3D_NVT_NHC_benchmark_suite.jl`, `benchmarks/NIST_LJ_Cuboid_HeadToHead.jl`, the bonded-exclusions slowdown guardrail, or something else?
8. Which Julia version or versions, CUDA.jl version or versions, and GPU architectures must remain supported during this run? I'm currently on Julia `1.12.3` with `CUDA.functional() == true`.
9. Baseline today is `490 passed / 1 failed` in `test/test_bonded_exclusions.jl`. Should I fix that first in a dedicated PR, or proceed with a known-red baseline and isolate only new regressions?
10. Is it acceptable to add CPU reference implementations in tests only, even if a full CPU backend is not being implemented now?
11. May I introduce new internal modules, files, or directories such as `src/SimulationState.jl`, `src/SimulationBuild.jl`, or `src/Backends.jl` if that is the smallest safe way to implement the high-priority recommendations?
12. How much backward compatibility do you want for current scripts and examples: preserve only where easy, preserve aggressively, or allow clean breaks with documentation?
13. If a compatibility shim would complicate the architecture but would not affect hot paths, do you still want it, or should I prefer a documented breaking change?
14. Should CI, workflow cleanup, and docs deployment changes be included now if they are high priority, or do you want infrastructure deferred unless it is required by code changes?
15. Is it acceptable to tighten or add `[compat]` entries, split docs/test/benchmark environments, or add weak dependencies or extensions if the report recommends it and the change is localized?
16. Should docs changes remain strictly minimal in each PR, or is one dedicated docs-sync PR allowed if documentation is part of the high-priority release-prep work?
17. What commit message style do you want: conventional-commit style, plain imperative sentences, or another existing repository convention?
18. Should the PRs be stacked on top of each other, or should each PR be rebased/merged against the base branch before the next PR starts?
19. If a report recommendation conflicts with measured performance or GPU residency, should I stop and ask first, or should I choose the higher-performance implementation and document the deviation?
20. What is the maximum total number of PRs you want in this run: `3`, `4`, `5`, `6`, or `7`? My draft above is `6`, with the first PR specifically addressing the current red baseline.
